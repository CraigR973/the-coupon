# Exact counts after Batch 167 (23 Sep 2026).
#
# ci-local.sh refuses both a fall and an unrecorded rise. When a batch adds
# tests, raise the applicable value to the count printed by the gate and rerun
# the whole gate. scripts/assert-quality-guardrails.sh permits increases and
# refuses reductions.
BACKEND_TEST_COUNT=1179
FRONTEND_TEST_COUNT=1122
