---
description: Tool-agnostic wrapper. Close a launch phase using docs/agent-commands/launch-closeout.md.
---

Read and follow:

```text
docs/agent-commands/launch-closeout.md
```

Pass through `$ARGUMENTS` unchanged.
