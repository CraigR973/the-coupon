---
description: Tool-agnostic wrapper. Verify a launch phase using docs/agent-commands/launch-verify.md.
---

Read and follow:

```text
docs/agent-commands/launch-verify.md
```

Pass through `$ARGUMENTS` unchanged.
